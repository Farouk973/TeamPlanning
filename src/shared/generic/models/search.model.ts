export interface searchmodel{
    optionEndpoint:string;
    label:string;
    input:input;
    addoption:boolean;
}
export interface input{
    type:string;
    aria_label:string;
    placeholder:string;
}